## QoE-Guard (validator server) runtime configuration
## Copy to .env and customize. Do NOT commit secrets.
##
## Note: .env.example is blocked in this environment, so we ship this template instead.

# Default target base URL used by demo buttons and as a fallback for scenarios
QOE_GUARD_TARGET_BASE_URL=http://127.0.0.1:8001

# Default endpoint path used by demo buttons and as a fallback for scenarios
QOE_GUARD_ENDPOINT=/play

# HTTP timeout (seconds) for fetching JSON from endpoints
QOE_GUARD_HTTP_TIMEOUT_SEC=15

# ============================================
# NOTIFICATIONS (Slack & Gmail)
# ============================================

# Slack Webhook URL (get from https://api.slack.com/messaging/webhooks)
# Format: https://hooks.slack.com/services/YOUR/WEBHOOK/URL
QOE_GUARD_SLACK_WEBHOOK=

# Gmail Configuration
# Your Gmail address
QOE_GUARD_GMAIL_USER=your-email@gmail.com
# Gmail App Password (generate at https://myaccount.google.com/apppasswords)
# NOT your regular Gmail password!
QOE_GUARD_GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
# Comma-separated list of recipient email addresses
QOE_GUARD_EMAIL_RECIPIENTS=team@example.com,devops@example.com

# ============================================
# OPTIONAL: Other Webhooks
# ============================================

# Discord Webhook URL
# QOE_GUARD_DISCORD_WEBHOOK=https://discord.com/api/webhooks/...

# Microsoft Teams Webhook URL
# QOE_GUARD_TEAMS_WEBHOOK=https://outlook.office.com/webhook/...

# Custom Webhook URL
# QOE_GUARD_CUSTOM_WEBHOOK=https://your-custom-webhook.com/endpoint

